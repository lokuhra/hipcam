import React from 'react';
// We want to always get from source
import { Button } from '../lib/freya-button';

export default { title: 'Button' };

export const primary = () => <Button>Hello Button</Button>;
